# NPM configuration for Mega Knights

# Allow legacy peer dependencies for Node 20.9 compatibility
legacy-peer-deps=true

# Audit settings: accept moderate vulnerabilities in dev-only dependencies
# Remaining vulnerabilities are ReDoS patterns in ESLint/TypeScript transitive deps
# These do not affect the built add-on, only the local dev environment
audit-level=moderate
